#!/usr/bin/env bash
# ----------------------------------------------------------------------
# Description
#   Load the given configuration for Gnome-panel.
#
# Arguments
#   1 (required) the name of the config to load. The file will be looked
#                up at $GNOME_PANEL_CONFS_HOME/<name>
#
# Dependencies
#   * Commands: gconftool.
# ----------------------------------------------------------------------


[[ -n $GNOME_PANEL_CONFS_HOME ]] || "\$GNOME_PANEL_CONFS_HOME is not defined."
gconftool --load $GNOME_PANEL_CONFS_HOME/$@

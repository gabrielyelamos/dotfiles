#!/bin/sh
# ----------------------------------------------------------------------
# Description
#   Toggle the touchpad off is a mouse is plugged, switch it off
#   otherwise.
# ----------------------------------------------------------------------

if lsusb --verbose 2>&1 | grep --quiet "Mouse"; then
  synclient TouchPadOff="1"
else
  synclient TouchPadOff="0"
fi

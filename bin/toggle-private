#!/usr/bin/env bash
# ----------------------------------------------------------------------
# Description
#   Mount/unmount the default private folder.
#
# Dependencies
#   * Scripts: toggle-truecrypt-folder
# ----------------------------------------------------------------------


[[ -n $PRIVATE_DIR ]] || "\$PRIVATE_DIR is not defined."
toggle-truecrypt-folder "$PRIVATE_DIR.tc" "$PRIVATE_DIR"
